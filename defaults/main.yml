# roles/nfs/defaults/main.yml
---
openio_nfs_namespace: "OPENIO"
openio_nfs_serviceid: "0"

openio_nfs_gridinit_dir: "/etc/gridinit.d/{{ openio_nfs_namespace }}"
openio_nfs_gridinit_file_prefix: ""

#openio_nfs_bind_interface: "{{ ansible_default_ipv4.alias }}"
#openio_nfs_bind_address:
#  "{{ hostvars[inventory_hostname]['ansible_' + openio_nfs_bind_interface]['ipv4']['address'] }}"

#openio_nfs_volume: "/var/lib/oio/sds/{{ openio_nfs_namespace }}/{{ openio_nfs_servicename }}"
openio_nfs_provision_only: false
openio_nfs_location: "{{ ansible_hostname }}.{{ openio_nfs_serviceid }}"
...
